# Generated by the AI Software Factory (internal/testharness) — do not edit: it
# is rewritten on every run. The tester reaches the stack only through the
# gateway, once the gateway is healthy. Run it with:
#   docker compose -f docker-compose.yml -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from tester
services:
  tester:
    build:
      context: ./test
    environment:
      APP_URL: http://gateway
      BASE_URL: http://gateway
    depends_on:
      gateway:
        condition: service_healthy
{{- if .E2E}}
    shm_size: 1gb
{{- end}}
{{- if .Networks}}
    networks:
{{- range .Networks}}
      - {{printf "%q" .}}
{{- end}}
{{- end}}
