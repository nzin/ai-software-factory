// Code generated by go-swagger; DO NOT EDIT.

package webhooks

import (
	"net/http"

	"github.com/go-openapi/runtime/middleware"
)

// GithubWebhookHandlerFunc turns a function with the right signature into a github webhook handler
type GithubWebhookHandlerFunc func(GithubWebhookParams) middleware.Responder

// Handle executing the request and returning a response
func (fn GithubWebhookHandlerFunc) Handle(params GithubWebhookParams) middleware.Responder {
	return fn(params)
}

// GithubWebhookHandler interface for that can handle valid github webhook params
type GithubWebhookHandler interface {
	Handle(GithubWebhookParams) middleware.Responder
}

// NewGithubWebhook creates a new http.Handler for the github webhook operation
func NewGithubWebhook(ctx *middleware.Context, handler GithubWebhookHandler) *GithubWebhook {
	return &GithubWebhook{Context: ctx, Handler: handler}
}

// GithubWebhook swagger:route POST /v1/webhooks/github webhooks githubWebhook
//
// # GitHub pull-request review webhook
//
// Verifies the X-Hub-Signature-256 HMAC, then turns a PR review or an inline review comment into the same re-entry the in-UI review does: a change request becomes a Finding{source:"human"} routed to the responsible developer; an approval accepts the run. There is no request body schema here on purpose — the handler reads the raw body itself for signature verification.
type GithubWebhook struct {
	Context *middleware.Context
	Handler GithubWebhookHandler
}

func (o *GithubWebhook) ServeHTTP(rw http.ResponseWriter, r *http.Request) {
	route, rCtx, _ := o.Context.RouteInfo(r)
	if rCtx != nil {
		*r = *rCtx
	}
	params := NewGithubWebhookParams()
	if err := o.Context.BindValidRequest(r, route, &params); err != nil { // bind params
		o.Context.Respond(rw, r, route.Produces, route, err)
		return
	}

	res := o.Handler.Handle(params) // actually handle the request

	o.Context.Respond(rw, r, route.Produces, route, res)

}
