/bin/
*.db
*.db-shm
*.db-wal
/browser/*/dist/
/browser/*/node_modules/
coverage.txt

# per-run git worktrees, the persistent local factory workspace (generated
# projects + their git history), and the cloned Kodus installer
/workspace/
/local_git/
/.kodus/

# local secrets / scratch (never commit)
anthropic_api_key
*_api_key
full_plan.md
.env
plan.md
